function Range(){

    const value = props.value;
    // const min = props.min;
    // const max = props.max;

    function something(){
        if(props.change){
            props.change(value);
        }
    }

    return (
       
    )
}

export default Range;